{% extends 'baseIndex.html.twig' %}

{% block title %}Hello AdministracionController!{% endblock %}

{% block body %}

<h1 class="mt-4">Detalles</h1>
<p>{{hotel.getNombre()}}</p>

<div class="card">
  <div class="card-body">
    <h5 class="card-title">Datos Generales</h5>
    <h4 class="card-subtitle mb-2 text-muted">Datos Generales</h6>
    <h6 class="card-text">Nombre</h4>
    <p>{{hotel.getNombre()}}</p>
     <h6 class="card-text">Telefono</h4>
    <p>{{hotel.getTelefono()}}</p>
       <h6 class="card-text">Actividad</h4>
    <p>{{hotel.getActividad()}}</p>
    <h4 class="card-subtitle mb-2 text-muted">Ubicacion</h6>
    <h6 class="card-text">Direccion</h4>
    <p>{{hotel.getUbicacion().getDireccion()}}</p>
    <h6 class="card-text">Comunidad</h4>
    <p>{{hotel.getUbicacion().getComunidad()}}</p>
    <h6 class="card-text">Provincia</h4>
    <p>{{hotel.getUbicacion().getProvincia()}}</p>
     <h4 class="card-subtitle mb-2 text-muted">Coodernadas</h6>
       <h6 class="card-text">Latitud</h4>
    <p>{{hotel.getUbicacion().getLatitud()}}</p>
    <h6 class="card-text">Longitud</h4>
    <p>{{hotel.getUbicacion().getLongitud()}}</p>


  </div>
</div>
     <a href="{{path('alojamientos')}}"><button class="btn btn-primary btn-lg" type="button" style="margin-left: 2rem">Volver</button></a>

{% endblock %}
