{% extends 'baseIndex.html.twig' %}

{% block title %}Hello AdministracionController!{% endblock %}

{% block body %}

<h1 class="mt-4">Editar</h1>
<p>{{hotel.getNombre()}}</p>

<div class="card">
  <div class="card-body">

<form class=" col-12 justify-content-center " action="{{ path('EditH', {'id':hotel.getId()})}}"  method="post" >
 <div class="p-5 pt-2  d-flex flex-column gap-3 justify-content-center col-8">

    <div class="form-group"> <!-- Full Name -->
        <label for="nombre" class="control-label">Nombre</label>
        <input type="text" class="form-control" id="nombre" name="nombre" value="{{hotel.getNombre()}}" placeholder="Introduce un nombre" >
    </div> 

    <div class="form-group"> <!-- Telefono -->
        <label for="nombre" class="control-label">Telefono</label>
        <input type="text" class="form-control" id="telefono" name="telefono" value="{{hotel.getTelefono()}}" placeholder="Introduce un nombre" >
     </div> 

      <div class="form-group"> <!-- Actividad -->
        <label for="nombre" class="control-label">Actividad</label>
        <input type="text" class="form-control" id="actividad" name="actividad" value="{{hotel.getActividad()}}" placeholder="Introduce un nombre" >
     </div>

       <div class="form-group"> <!-- caracteristicas -->
        <label for="nombre" class="control-label">Caracteristicas</label>
        <input type="text" class="form-control" id="caracteristicas" name="caracteristicas" value="{{hotel.Caracteristicas()}}" placeholder="Introduce un nombre" >
     </div> 

           <div class="form-group"> <!-- descripcion -->
        <label for="nombre" class="control-label">Descripcion</label>
        <input type="text" class="form-control" id="descripcion" name="descripcion" value="{{hotel.Descripcion()}}" placeholder="Introduce un nombre" >
     </div> 

      <div class="form-group"> <!-- Actividad -->
        <label for="nombre" class="control-label">Actividad</label>
        <input type="text" class="form-control" id="actividad" name="actividad" value="{{hotel.getActividad()}}" placeholder="Introduce un nombre" >
     </div>

       <div class="form-group"> <!-- direccion -->
        <label for="nombre" class="control-label">Direccion</label>
        <input type="text" class="form-control" id="direccion" name="direccion" value="{{hotel.getUbicacion().getDireccion()}}" placeholder="Introduce un nombre" >
     </div>

     <div class="form-group"> <!-- Comunidad -->
        <label for="nombre" class="control-label">Comunidad</label>
        <input type="text" class="form-control" id="comunidad" name="comunidad" value="{{hotel.getUbicacion().getComunidad()}}" placeholder="Introduce un nombre" >
     </div>
     <div class="form-group"> <!-- Provincia -->
        <label for="nombre" class="control-label">Provincia</label>
        <input type="text" class="form-control" id="provincia" name="provincia" value="{{hotel.getUbicacion().getProvincia()}}" placeholder="Introduce un nombre" >
     </div>

      <div class="form-group"> <!-- Latitud -->
        <label for="nombre" class="control-label">Latitud</label>
        <input type="text" class="form-control" id="latitud" name="latitud" value="{{hotel.getUbicacion().getLatitud()}}" placeholder="Introduce un nombre" >
     </div>
     <div class="form-group"> <!-- Longitud -->
        <label for="nombre" class="control-label">Longitud</label>
        <input type="text" class="form-control" id="longitud" name="longitud" value="{{hotel.getUbicacion().getLongitud()}}" placeholder="Introduce un nombre" >
     </div>

 <div class="form-group"> <!-- Submit Button -->
        <a href="{{path('alojamientos')}}"><button type="button" class="btn btn-danger">Cancelar</button></a>
        <button type="submit" class="btn btn-primary" name="editar">Editar</button>
    </div> 
</form>
</div>
</div>

   

{% endblock %}