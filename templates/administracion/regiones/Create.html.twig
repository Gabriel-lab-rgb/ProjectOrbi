{% extends 'baseIndex.html.twig' %}

{% block title %}Hello AdministracionController!{% endblock %}

{% block body %}


<h1 class="mt-5">Crear</h1>


{{ form_start(CreateForm) }}
   <div class="p-5 pt-2  d-flex flex-column gap-3 justify-content-center col-8 w-100">

<div class="card ">
  <div class="card-header ">
    <h3 class="card-title fw-light">Datos de Region</h3>
  </div>
  <div class="card-body">
  
  <div class="form-group"> <!-- Full Name -->
       {{ form_label(CreateForm.nombre) }}
       {{ form_widget(CreateForm.nombre) }}
    </div> 

 <div class="form-group"> <!-- latitud -->
         {{ form_label(CreateForm.latitud) }}
         {{ form_widget(CreateForm.latitud) }}
     </div>

  <div class="form-group"> <!-- longitud -->
         {{ form_label(CreateForm.longitud) }}
        {{ form_widget(CreateForm.longitud) }}
     </div> 

           <div class="form-group"> <!-- descripcion -->
        {{ form_label(CreateForm.descripcion) }}
        {{ form_widget(CreateForm.descripcion) }}
     </div> 
</div>
</div>




   
 <div class="form-group"> <!-- Submit Button -->
        <a href="{{path('region')}}"><button type="button" class="btn btn-danger">Cancelar</button></a>
        <button type="submit" class="btn btn-primary" name="editar">Crear</button>
    </div> 
 {{form_end(CreateForm)}}



{% endblock %}