{% extends 'baseIndex.html.twig' %}



{% block body %}

{% for reserva in reservas %}


<div class="card mb-3" style="max-width: 540px;">
  <div class="row g-0">
    <div class="col-md-4">
      <img src="{{asset('/img/alojamientos/' ~ reserva.getHotel().Images[0].ImageName)}}" class="img-fluid rounded-start" alt="...">
    </div>
    <div class="col-md-8">
      <div class="card-body">
        <h5 class="card-title">{{reserva.getHotel().getNombre()}}</h5>
         <h4 class="card-title">{{reserva.getFecha()|date('d-m-Y')}}</h4>
        
        <p>llegada :{{reserva.getLlegada()|date('d-m-Y') }}</p>
        <p>Salida :{{reserva.getLlegada()|date('d-m-Y') }}</p>
        <p class="card-text"><small class="text-muted">Adultos:{{reserva.getAdultos()}}</small></p>
        <p class="card-text"><small class="text-muted">Niños:{{reserva.getNinos()}}</small></p>
        
      </div>
    </div>
  </div>
</div>


{% endfor %}


{% endblock %}